export interface AuthState {
    userid: string,
    name: string,
    isLoaded: boolean
}

export interface LivePriceState {
    inr: number,
    usd: number,
    eur: number,
    isLoaded: boolean
}